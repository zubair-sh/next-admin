version: 1
frontend:
  phases:
    preBuild:
      commands:
        - yarn install
        - npx prisma generate
    build:
      commands:
        - npx prisma migrate deploy
        - yarn build
  artifacts:
    baseDirectory: .next
    files:
      - "**/*"
  cache:
    paths:
      - node_modules/**/*
